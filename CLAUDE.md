# Life Guardian — 프로젝트 컨텍스트

## 이게 뭔가

삼성생명 **LIFENOLOGY LAB 3기 공모전** 출품작. 주제는 "보험을 넘어서는 보험" —
10년 뒤 보험회사가 어떤 서비스를 해야 하는지를 제안한다.

우리 제안: **생체신호로 '나도 몰랐던 나'를 읽어, 관계·직업 스트레스를 줄이는 개인 최적화 솔루션.**
이 저장소는 그 비전을 보여주는 **인터랙티브 웹 데모**다. 공모전 제출물이자 부스 시연 도구.

## 필독 문서 (작업 전 반드시 읽을 것)

| 파일 | 내용 |
|------|------|
| `상세기획안.md` | 기획 원본. 체험 시나리오(Phase 1~4), 자극 사진쌍, 대화 스크립트, **LLM 프롬프트 템플릿(부록 C)**, 윤리 가드레일 |
| `구현계획.md` | 기술 구현 계획. 아키텍처, 센서 연동, 씬 설계, 디자인 방향, 마일스톤 M1~M6 |

`상세기획안.md` 부록 C의 **Input/Output JSON 스키마와 System 프롬프트는 그대로 구현**한다. 임의로 바꾸지 않는다.

## 핵심 원칙

1. **LLM은 해석만 한다.** 점수 계산(시선 가중합, 그림 4축 환산, 불일치 지수)은 전부 TypeScript 코드에서 끝내고 결과 JSON만 LLM에 넘긴다.
2. **센서는 인터페이스 뒤에 숨긴다.** 실기기가 아직 배송 전이므로 모든 신호는 `DataSource` 추상화를 거친다. 구현체는 시뮬레이션 / 수동입력 / 실기기 3종.
3. **데모 모드가 기본값이다.** 심사위원은 센서 없이 URL만 연다. 센서 없이도 전체 플로우가 완주되어야 한다.
4. **윤리 가드레일은 코드로 강제한다.** 진단명·단정형 표현 금지, 음성 원본 미저장, 중단 규칙. `상세기획안.md` 부록 B/C 참조.
5. **디자인은 "의료기기의 신뢰감 × 명상 앱의 고요함".** AI 티 나는 보라 그라데이션·글래스모피즘 금지. 딥 차콜 배경 + 생체신호 색만 포인트.

## 기술 스택

- Next.js 15 (App Router) + TypeScript + Tailwind CSS v4
- Zustand (세션 상태 머신), Framer Motion (모션), Canvas 2D (파형 렌더링)
- Supabase 무료 티어 (Postgres + Realtime) — 세션 저장, 화면 간 동기화
- Anthropic API — 해석 `claude-opus-5`, 발화 정서가 평정은 저렴한 모델
- 센서: Web Bluetooth (밴드 HR/HRV) / Web Serial + Arduino (Grove GSR) / WebGazer.js (웹캠 시선) / Web Speech API (STT·TTS)
- 배포: Vercel — **제출용 URL과 부스 실행 URL이 동일**

## 환경변수

`.env.local` (커밋 금지):

```
ANTHROPIC_API_KEY=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
```

키가 없어도 앱은 폴백(규칙 기반 템플릿 + 로컬 저장)으로 동작해야 한다. 데모가 키에 인질로 잡히면 안 된다.

## 작업 규칙

- 커밋은 마일스톤/기능 단위로 쪼갠다. 한 번에 몰아서 하지 않는다.
- `npm run build`와 타입체크가 통과하지 않으면 커밋하지 않는다.
- 순수 함수(FeatureEngine)는 유닛 테스트를 함께 작성한다.
- 한국어 UI. 코드 주석·커밋 메시지도 한국어 허용.
