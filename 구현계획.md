# Life Guardian — 웹 데모 구현 계획

> 상세기획안.md의 부스 체험(Phase 1~4)을 실제로 돌아가는 인터랙티브 웹 데모로 구현하기 위한 기술 계획서.
> 작성일: 2026-08-10

---

## 0. 핵심 설계 판단 (TL;DR)

| 결정 | 선택 | 이유 |
|------|------|------|
| 프레임워크 | **Next.js (App Router) + TypeScript** | 프론트+백엔드(API Route)를 한 프로젝트로. 무료, Vercel 무료 배포 |
| 배포 | **Vercel 무료 티어 (제출용 공개 URL)** | 심사위원 누구나 접속해 시뮬레이션 모드로 전체 체험 가능. HTTPS라 Web Bluetooth/Serial/웹캠 전부 동작 → 부스에서도 같은 URL 사용 |
| 백엔드/DB | **Next.js API Route + Supabase 무료 티어** (Postgres + Realtime) | 세션 데이터 영구 저장, 참가자↔진행자 화면 실시간 동기화, 갤러리·QR 소장 기능의 기반 |
| 스마트밴드 연동 | **Web Bluetooth API** (브라우저에서 BLE 직접 연결) | 표준 Heart Rate Service(0x180D)로 HR + RR-interval 수신 → HRV(RMSSD) 브라우저에서 계산. 앱·서버 불필요 |
| GSR 연동 | **Arduino + Web Serial API** | Grove GSR은 아날로그 센서라 MCU 필수. 아두이노가 시리얼로 값을 쏘고 브라우저가 직접 읽음 |
| 아이트래커 | **WebGazer.js (웹캠 기반 시선추적)** | 미구매 상태인 아이트래커를 웹캠으로 대체 가능. 무료, 브라우저 내장, 하드웨어 0원 |
| 센서 없을 때 | **시뮬레이션 레이어 (Source 추상화)** | 모든 센서를 `DataSource` 인터페이스 뒤에 숨김. 실기기/시뮬레이터/수동입력을 토글로 교체 |
| LLM | **Anthropic API (claude-opus-5)** via API Route | 프롬프트는 부록 C 그대로. API 키는 서버사이드에만 보관 |
| 영수증 | **@page CSS로 80mm 프린트** (감열 영수증 프린터) | 브라우저 인쇄로 처리. ESC/POS 직접 제어는 2차 옵션 |
| 실시간 그래프 | **Canvas 직접 렌더링** | 심전도 모니터 느낌의 부드러운 스크롤 파형. 차트 라이브러리보다 아름답고 가벼움 |

⚠️ **중요 확인 필요 1**: "Claude Pro" 구독은 API 사용과 별개입니다. LLM 호출에는 [console.anthropic.com](https://console.anthropic.com)에서 발급한 **API 키 + 크레딧(최소 $5)**이 필요합니다. 1인당 1회 호출이면 체험 100명 기준 몇 달러 수준.

⚠️ **중요 확인 필요 2**: 저가 스마트밴드는 표준 BLE Heart Rate Service를 제공하지 않는 경우가 많습니다(전용 앱만 지원). 밴드 도착 즉시 **nRF Connect 앱으로 `0x180D` 서비스 노출 여부를 확인**해야 합니다. 미지원이면: ① 갤럭시워치/애플워치 등 표준 지원 기기로 대체, ② 밴드 전용 프로토콜 리버싱(비추천), ③ 시뮬레이션 유지 중 택1.

---

## 1. 전체 아키텍처

### 1.0 데이터 흐름의 역할 분담 — "수집은 브라우저, 저장·동기화는 백엔드"

센서 실시간 수집은 브라우저가 직접 한다(Web Bluetooth/Serial은 브라우저 전용 API라 서버가 대신 받을 수 없음).
백엔드(Supabase)가 맡는 것은 그 다음이다:

1. **영구 저장** — 초당 수십 샘플의 원시 신호는 브라우저 메모리에 버퍼링하고, 5초 단위 요약 청크 + 이벤트 마커 + 단계별 특징값을 Supabase에 적재. 세션 종료 시 전체 결과 JSON 저장
2. **화면 간 실시간 동기화** — 참가자 화면(대형 디스플레이)과 진행자 대시보드(노트북)가 서로 다른 기기여도 Supabase Realtime 채널로 상태·파형 공유
3. **LLM 프록시** — API 키는 Vercel 서버 환경변수에만 존재
4. **체험 후 소장** — 영수증의 QR → `/receipt/[sessionId]`에서 본인 결과를 다시 열람 (저장돼 있으니 가능)

```
┌──────────────────────── 어디서든 (제출 URL = 부스 URL) ────────────────────────┐
│                                                                               │
│  [센서 계층]                    [Next.js 앱]                                    │
│                                                                               │
│  스마트밴드 ──BLE──▶ Web Bluetooth ─┐                                          │
│  Grove GSR ─Arduino─▶ Web Serial ──┤   SensorHub (스트림 통합·타임스탬프 정렬)    │
│  웹캠 ──────────▶ WebGazer.js ─────┤        │                                  │
│  마이크 ────────▶ Web Speech API ──┘        ▼                                  │
│                                    FeatureEngine (baseline 정규화·점수 계산)    │
│  ┌─ 시뮬레이터 (기기 없을 때) ─┐              │                                  │
│  │ 가상 참가자 페르소나 4종    │              ▼                                  │
│  │ + 수동 조작 슬라이더       │       /api/interpret ──▶ Anthropic API          │
│  └──────────────────────────┘              │                                  │
│                                            ▼                                  │
│   [화면 1] 참가자용 체험 화면        bio-receipt 렌더 ──▶ 영수증 프린터(80mm)      │
│   [화면 2] 진행자용 대시보드  ◀─Realtime─▶ Supabase (Postgres + Realtime 채널)   │
└───────────────────────────────────────────────────────────────────────────────┘
```

- **모든 신호 수집이 브라우저에서 직접** 일어난다 (Web Bluetooth / Web Serial / WebGazer / Web Speech 전부 Chrome 지원, HTTPS 배포 환경에서 동작).
- 저장 스키마(Supabase): `sessions`(세션 메타·최종 결과 JSON), `signal_chunks`(5초 요약 파형), `events`(자극 제시·질문 시작 등 마커), `receipts`(LLM 출력). 발화 음성 원본은 저장하지 않음 — 기획안 윤리 가드레일 준수.
- Supabase 무료 티어(500MB DB, Realtime 포함)로 충분: 1세션당 데이터 ≈ 수백 KB 수준.
- 인터넷이 끊긴 부스 상황 폴백: 브라우저 IndexedDB에 로컬 큐잉 후 재접속 시 업로드 (데모가 네트워크에 인질로 잡히지 않게).

## 2. 센서 연동 상세

### 2.1 DataSource 추상화 — 이 프로젝트의 뼈대

기기가 아직 배송 전이므로, **모든 코드는 인터페이스에만 의존**하게 만든다. 기기가 도착하면 구현체 하나만 갈아끼우면 됨.

```typescript
interface BioSample { t: number; hr?: number; rr?: number[]; gsr?: number; }

interface DataSource {
  readonly kind: 'band' | 'gsr' | 'gaze';
  readonly mode: 'live' | 'simulated' | 'manual';
  connect(): Promise<void>;
  subscribe(cb: (s: BioSample) => void): () => void;
  status: 'idle' | 'connecting' | 'streaming' | 'error';
}
```

구현체 3종 × 센서별:

| 구현체 | 설명 |
|--------|------|
| `SimulatedSource` | **기본값.** 생리학적으로 그럴듯한 신호 생성기. HR: 평균 72bpm + 호흡성 동성부정맥(0.25Hz sinus) + 노이즈. GSR: 토닉 레벨 + 이벤트 트리거 시 SCR 파형(1~3초 상승, 지수 감쇠 회복). **체험 이벤트(자극 제시, 질문 시작)에 반응하도록 이벤트 버스와 연결** — 데모가 "진짜처럼" 반응함 |
| `ManualSource` | 진행자 대시보드의 슬라이더/버튼으로 실시간 조작. "지금 각성 스파이크 발생" 버튼 등. 발표 시연 때 극적 연출용 |
| `BleHeartRateSource` / `SerialGsrSource` / `WebGazerSource` | 실기기. 아래 상세 |

### 2.2 스마트밴드 (HR·HRV) — Web Bluetooth

- 표준 GATT **Heart Rate Service (0x180D)** → `heart_rate_measurement` characteristic notify 구독.
- 측정값 플래그에 **RR-interval 포함 여부**가 있음 → RR이 오면 **RMSSD를 브라우저에서 직접 계산** (30초 슬라이딩 윈도우).
- RR 미제공 밴드면 HR만 쓰고 HRV는 HR 변동성으로 근사(품질 플래그 `degraded` — 프롬프트 규칙과 연동됨).
- 연결 UX: 진행자 대시보드에서 "밴드 연결" 버튼 → Chrome 기기 선택 팝업 → 연결 후 신호 품질 인디케이터 표시.

### 2.3 Grove GSR — Arduino + Web Serial

- Grove GSR은 순수 아날로그 출력 → **Arduino Uno/Nano의 A2**에 연결 (스케치의 `GSR_PIN`과 일치해야 함).
- 아두이노 스케치 (레포에 포함): 20ms 간격(50Hz)으로 `t,raw` CSV 라인을 115200bps 시리얼 출력. 펌웨어에서 이동평균 5샘플 스무딩.
- 브라우저에서 `navigator.serial.requestPort()` → 라인 파싱 → μS 단위 변환.
- 전처리(브라우저): 토닉(SCL) = 저역통과, 페이직(SCR) = 미분 기반 피크 검출. 이벤트 마커 후 1~4초 창의 SCR 진폭 귀속(기획안 그대로).

### 2.4 아이트래커 — WebGazer.js (웹캠)

- BOM에서 아이트래커가 미정 상태 → **WebGazer.js**(웹캠 기반, MIT 라이선스, 무료)로 해결.
- 체험 시작 시 9점 캘리브레이션(점을 따라 클릭) — 이것 자체가 재미있는 인터랙션.
- 정밀도는 상용 아이트래커보다 낮지만, 좌/우 이미지 중 **어느 쪽을 보는지** 판별에는 충분함 (화면 절반 단위 판정).
- 산출: first fixation(0.5초 내 첫 응시 방향), dwell ratio(좌우 체류 비율) → 기획안 공식 `0.4×첫응시 + 0.6×체류비율, 각성 동반 ×1.3` 그대로 코드화.
- 폴백: 웹캠 불가 환경에선 **마우스 호버**를 시선 프록시로 사용 (데모 배포판에서 유용).

### 2.5 그림 (Session 2) — 포인터 이벤트 캔버스

- `<canvas>` + Pointer Events. 태블릿/아이패드 연결 시 `pressure` 속성으로 실제 필압 수집, 마우스면 속도 기반 근사.
- 자동 코딩 (기획안 체크리스트 → 전부 계산 가능):
  - 크기: 스트로크 바운딩박스 / 캔버스 면적
  - 위치: 무게중심의 상하좌우 편향
  - 필압·선: 평균 pressure, undo/지우개 횟수
  - 세부 요소: 스트로크 클러스터 수로 근사
  - 착수 지연·총 소요시간: 타임스탬프
- 그리는 동안 GSR/HR 타임라인과 스트로크 타임스탬프를 매핑 → "뿌리를 그릴 때 각성 상승" 검출.
- 구조(뿌리/줄기/수관 유무)는 자동 판별이 어려우므로 **그림 완료 후 진행자가 대시보드에서 3초 만에 체크**(체크박스 3개).

### 2.6 대화 (Session 3) — Web Speech API

- Chrome 내장 STT(`webkitSpeechRecognition`, 한국어 지원, 무료)로 실시간 텍스트 변환.
- 질문 낭독: Web Speech Synthesis(TTS) 또는 사전 녹음 음성 파일 재생 — 기획안의 "낭독 통일" 원칙에 부합.
- 타이밍 자동 기록: 낭독 종료 마커 → 첫 발화 감지까지 = 응답 지연. 어절 수, 침묵 구간 카운트.
- `content_valence`는 기획안대로 **별도 LLM 호출**(haiku급 모델로 저렴하게)로 선평정 후 메인 프롬프트에 주입.
- 음성 원본은 저장하지 않음 (STT 결과 텍스트만 유지).

## 3. 화면 구성 & UX 플로우

### 3.1 라우트 구조

```
/                    → 랜딩 (프로젝트 비전 소개, "체험 시작")
/experience          → 참가자용 풀스크린 체험 (Phase 1~4를 한 플로우로)
/operator            → 진행자 대시보드 (별도 창/기기, SSE 동기화)
/receipt/[sessionId] → bio-receipt 화면 + 인쇄
/gallery             → (선택) 오늘의 체험자 페르소나 벽 — 부스 유인용
```

### 3.2 참가자 체험 플로우 (풀스크린, 단계별 몰입형)

각 단계는 **풀스크린 씬 전환**으로 진행. 진행바는 상단에 가늘게. 애플 온보딩처럼 "한 화면 = 한 가지 일".

| 씬 | 화면 | 인터랙션 & 연출 |
|----|------|----------------|
| **S0 인트로** | 검은 화면에 심장박동 리듬으로 맥동하는 타이포: "당신의 몸은 이미 알고 있습니다" | 터치/클릭으로 시작. 이름(닉네임) 입력 |
| **S1 장비 연결** | 각 센서가 카드로 표시, 연결되면 카드에 실제 파형이 흘러들어옴 | 밴드 연결 순간 화면 전체에 참가자의 심박이 살아나는 순간 = 첫 "와우" 포인트 |
| **S2 베이스라인** | 화면 중앙 큰 호흡 원(들숨/날숨 가이드), 배경에 본인 심박 파형이 은은히 흐름 | 60초. HRV·GSR 수렴 판정되면 원이 부드럽게 안정 색으로 전환 |
| **S3 명상** | 화면 최소화(안대 착용 가정), 오디오 페이싱. 데모 모드에선 파형이 잦아드는 비주얼 | 안정 판정 로직: 30초 창에서 HR 표준편차 < 임계 & SCL 기울기 ≈ 0 |
| **S4 시선 세션** | 캘리브레이션(움직이는 점 따라가기) → 12쌍 이미지 좌우 제시(6초) + 응시점(2초) | 참가자는 그냥 보기만 하면 됨. 좌우 무작위 반전. **자기 시선이 측정되고 있다는 것 자체가 체험** |
| **S5 그림 세션** | 종이 질감의 캔버스, 연필 소리 앰비언트. "나무 한 그루를 그려주세요" (90초) → "10년 뒤 나의 하루" (30초) | 남은 시간은 원형 게이지로 은은히. 압박감 없는 카운트다운 |
| **S6 대화 세션** | 화면에 파도 같은 보이스 비주얼라이저. 질문이 음성+자막으로 제시 | 발화하면 비주얼라이저가 반응. 5문항 × 30초 리듬 자동 진행 |
| **S7 해석 대기** | **하이라이트 씬.** LLM 대기 20~30초 동안 본인의 전체 세션 생체신호 타임라인이 시네마틱하게 리플레이: "이 사진에서 심박이 뛰었습니다" 마커들 | 대기시간을 지루함이 아니라 자기 데이터 관람으로 전환 (기획안 Phase 4 의도 그대로) |
| **S8 결과** | persona_name이 타이핑 애니메이션으로 등장 → 무의식 요약 3줄이 순차 페이드인 → hidden_finding은 심장박동 이펙트와 함께 → 추천 카드 4장 플립 | "인쇄" 버튼 → 영수증 출력. QR로 웹 버전 소장 (선택) |

### 3.2.1 "지루하지 않은가" 자가 점검 — 씬별 몰입 장치

10분 체험에서 지루함이 생길 수 있는 구간은 사실상 4곳이고, 각각 전용 장치를 설계했다:

| 위험 구간 | 왜 지루해질 수 있나 | 몰입 장치 |
|-----------|--------------------|----------|
| S2 베이스라인 (60초) | "그냥 앉아서 기다림" | 호흡 원을 **따라 하게** 만듦(수동적 대기 → 능동적 과제). 배경에 본인 심박이 실시간으로 흐르고, 안정될수록 화면 색온도가 미묘하게 가라앉음 — 내 몸이 화면을 바꾸는 경험 |
| S4 시선 세션 (2분 30초) | 사진을 보기만 함 | 보는 것 자체가 측정임을 화면 가장자리의 미세한 시선 트레이서로 암시 (단, 자극 응시를 방해하지 않게 응시점 구간에만 표시). 12쌍이 8초 리듬으로 계속 바뀌므로 체감상 빠름 |
| S7 LLM 대기 (20~30초) | 로딩 스피너의 함정 | **대기가 아니라 리플레이 쇼**: "3번째 사진에서 심박이 12% 뛰었습니다" 같은 마커가 타임라인 위로 순차 등장. 자기 데이터 관람은 이 데모에서 가장 재미있는 콘텐츠 |
| S8 결과 | 텍스트 덤프가 되기 쉬움 | 페르소나명 타이핑 연출 → 요약 3줄 순차 등장 → 추천 카드 플립 → 인쇄. 정보를 한 번에 쏟지 않고 리듬을 만들어 공개 |

그 외 씬은 본질적으로 능동 과제(그리기·말하기·연결하기)라 지루함 리스크가 낮다. 리허설에서 씬별 체감 시간을 실측해 조정한다.

### 3.3 진행자 대시보드 (/operator)

- 실시간 3채널 파형 (HR / HRV / GSR) + 이벤트 마커 타임라인
- 세션 단계 강제 이동 / 일시정지 / 중단 버튼 (윤리 가드레일: GSR 급등 30초 미회복 시 화면에 경고 배너)
- 시뮬레이션 모드 패널: 가상 페르소나 선택(4종: 조용한 탐구자 / 무대 위의 리더 / 자연을 그리는 사람 / 정돈된 설계자), 각성 이벤트 수동 트리거 슬라이더
- 그림 구조 체크(뿌리/줄기/수관), 대화 NR 기록 버튼
- LLM 결과의 `evidence` 배열 표시 (기획안: 영수증엔 미인쇄, 진행자 화면 전용)

### 3.4 bio-receipt

- 80mm 감열 프린터 기준 `@page { size: 80mm auto }` 인쇄 CSS.
- 레이아웃(부록 D 채움 제안): 상단 로고+세션ID → persona_name(큰 글씨) → one_liner → 구분선 → "오늘 당신의 몸이 말한 것" 3줄 → hidden_finding 박스 → 추천 4종(직업/커뮤니티/활동/취미 각 1~2) → tuning_quest("오늘의 처방") → 미니 심박 파형 그래픽(실제 본인 데이터) → disclaimer → 바코드풍 장식(세션ID 인코딩).
- **본인의 실제 파형이 영수증에 인쇄되는 것**이 기념품 가치의 핵심.

## 4. 디자인 방향

> "의료기기의 신뢰감 × 명상 앱의 고요함". AI 티 나는 보라색 그라데이션·글래스모피즘 금지.

- **컬러**: 거의 무채색. 딥 차콜(#0A0A0C) 배경 + 웜 화이트 텍스트. 포인트는 단 하나 — 생체신호 색(심박: 앰버/코랄, GSR: 틸, HRV: 세이지). 삼성생명 블루는 로고와 마지막 결과 씬에만 절제해서 사용.
- **타이포**: Pretendard (한글 최적화, 무료). 씬 타이틀은 크고 얇게, 데이터 수치는 tabular-nums.
- **모션**: 모든 전환은 심박·호흡 리듬에서 파생 (호흡 원 4초 주기, 페이드는 ease로 통일). Framer Motion. 놀라게 하는 모션 금지, 안내하는 모션만.
- **파형 렌더링**: Canvas 2D로 직접 그린 스크롤 파형 + 잔광(glow) 효과. 병원 모니터가 아니라 플라네타리움처럼.
- **사운드**: 은은한 앰비언트 + 단계 전환 차임 (선택, AirPods Max 활용).
- 다크 단일 테마 (부스 조명 환경에서 몰입감 + 파형 대비 극대화). 영수증/인쇄 뷰만 라이트.

## 5. 데이터 파이프라인 (기획안 → 코드 매핑)

기획안 부록 C 원칙 "**LLM은 해석만, 계산은 코드가**"를 그대로 구현:

```
SensorHub (수집)
  → SessionRecorder (타임스탬프 정렬, 이벤트 마커, JSON 저장)
  → FeatureEngine (전부 순수 함수, 유닛 테스트 대상)
      ├─ baseline.ts      : Phase1 60초 → hr_mean, hrv_rmssd, gsr_scl_mean
      ├─ gaze.ts          : 쌍별 first fixation + dwell → RIASEC 6축, 성향 6축 (-1~+1)
      ├─ drawing.ts       : 스트로크 통계 → 4축 (개방성/안정지향/표현욕구/완벽주의)
      ├─ dialogue.ts      : 지연·어절수·SCR z-score·불일치 지수 |valence − arousal|
      └─ crosscheck.ts    : gaze RIASEC vs 발화 RIASEC 일치 여부
  → buildLlmInput() : 부록 C Input 스키마 JSON 생성 (결측은 null + quality:"missing")
  → /api/interpret  : system 프롬프트(부록 C) + zod 스키마 검증 + 실패 시 1회 재시도
  → 폴백: 2회 실패 시 규칙 기반 템플릿 문장 (기획안 명시사항)
```

## 6. 기술 스택 최종안 (전부 무료)

| 레이어 | 선택 | 비용 |
|--------|------|------|
| 프레임워크 | Next.js 15 + TypeScript + Tailwind CSS v4 | 무료 |
| 상태관리 | Zustand (세션 상태 머신) | 무료 |
| 애니메이션 | Framer Motion | 무료 |
| 파형/차트 | Canvas 2D 직접 구현 | 무료 |
| 시선추적 | WebGazer.js | 무료 |
| STT/TTS | Web Speech API (Chrome 내장) | 무료 |
| 센서 | Web Bluetooth + Web Serial (Chrome 내장) | 무료 |
| 스키마 검증 | zod | 무료 |
| LLM | Anthropic API — 해석: claude-opus-5 1회/인, valence 평정: haiku | **유일한 유료** (체험 1인당 ~수십 원) |
| DB·실시간 동기화 | Supabase 무료 티어 (Postgres + Realtime) | 무료 |
| 실행 환경 | Vercel 무료 배포 — **제출용 URL과 부스 실행 URL이 동일** | 무료 |
| 프린터 | 브라우저 인쇄 → 80mm 감열 프린터 드라이버 | 프린터 하드웨어만 |

**브라우저는 Chrome 고정** (Web Bluetooth/Serial이 Chrome 계열 전용). Vercel 배포는 HTTPS이므로 센서 API가 그대로 동작한다.

### 제출 모드 (심사위원 관점)

심사위원은 센서 없이 URL만 열어본다. 따라서:
- 첫 화면에서 **"데모 모드로 체험하기"**가 기본 경로 — 시선은 마우스/터치, 생체신호는 시뮬레이터가 체험 이벤트에 반응하며 흐름
- 전체 10분 플로우를 **3분 압축 모드**로도 제공 (씬별 시간 단축 토글) — 심사 시간이 짧을 것에 대비
- 랜딩 페이지에 비전 스토리텔링(10년 뒤 가정 → 최적화 문제 → 체험) 포함해, 데모 링크 하나가 곧 제안서 역할을 하도록

## 7. 구현 로드맵

| 마일스톤 | 내용 | 완료 기준 |
|----------|------|----------|
| **M1. 골격 + 시뮬레이터** | Next.js 셋업, DataSource 추상화, 시뮬레이션 신호 생성기, 세션 상태 머신 | 가상 참가자로 Phase 1→4 전체 플로우가 자동 진행됨 |
| **M2. 체험 씬 (S0~S6)** | 풀스크린 씬 8개, 파형 렌더러, 호흡 가이드, 시선 세션(마우스 프록시), 캔버스 그림, 대화 UI | 사람이 처음부터 끝까지 재미있게 체험 가능 |
| **M3. 해석 엔진** | FeatureEngine 전체, LLM API Route, 프롬프트(부록 C), 폴백, S7·S8 결과 씬 | 시뮬레이션 데이터 → 그럴듯한 bio-receipt JSON 출력 |
| **M4. 배포 + 대시보드 + 영수증** | Supabase 스키마·Realtime 동기화, Vercel 배포(제출 URL 확보), /operator 수동 조작 패널, 인쇄 CSS, 갤러리, 3분 압축 모드 | 공개 URL에서 2화면 데모 리허설 가능 |
| **M5. 실기기 연동** (배송 후) | BLE HR 소스, Arduino 스케치 + Serial 소스, WebGazer 캘리브레이션 | 실측 데이터로 전체 플로우 1회 완주 |
| **M6. 폴리시** | 사운드, 마이크로 인터랙션, 자극 이미지 확정 반영, 리허설 체크리스트(부록 C 검수) 수행 | 공모전 시연 리허설 통과 |

M1~M4는 기기 없이 지금 바로 진행 가능. M5는 인터페이스가 이미 고정돼 있어 구현체 교체만 하면 됨.

## 8. 리스크 & 대응

| 리스크 | 확률 | 대응 |
|--------|------|------|
| 스마트밴드가 표준 BLE HR 미지원 | **높음** | 도착 즉시 nRF Connect로 확인. 미지원 시 갤럭시워치(표준 지원) 대체 또는 시뮬레이션 유지. 데모 스토리상 "미래엔 뉴럴칩" 프레임이라 시뮬레이션도 설득력 있음 |
| Claude Pro ≠ API 접근 | 확정 | API 키 발급 + $5 크레딧 충전 필요 (팀 결정 사항) |
| WebGazer 정밀도 부족 | 중간 | 좌/우 절반 판정만 사용 + 캘리브레이션 강화. 폴백: 마우스/터치 |
| 부스 네트워크 불안정 | 중간 | LLM 호출만 인터넷 필요 → 핫스팟 백업. 폴백 템플릿은 오프라인 동작 |
| STT 한국어 인식 오류 (소음 환경) | 중간 | 지향성 마이크 사용 + 진행자가 대시보드에서 텍스트 즉석 수정 가능하게 |
| 브라우저 권한 팝업이 체험 흐름 끊음 | 낮음 | 세션 시작 전 진행자가 모든 권한(BT/Serial/캠/마이크) 일괄 연결하는 프리플라이트 화면 |
