# 클라우드 세션 작업지시서

> 이 파일은 Claude Code on the web(claude.ai/code) 세션이 이 저장소를 이어받아
> 자율적으로 구현을 완주하기 위한 지시서다.

## 시작 전

1. `CLAUDE.md` → `상세기획안.md` → `구현계획.md` 순서로 읽는다.
2. 부록 C(LLM 프롬프트 템플릿)의 Input/Output 스키마는 **그대로** 구현한다.
3. 작업은 M1 → M6 순차 진행. 각 마일스톤이 끝나면 **빌드·테스트 통과 후 커밋**하고 다음으로 넘어간다.

## 모델 사용 지침 (토큰 효율)

메인 세션은 판단이 필요한 일만 직접 하고, 기계적인 작업은 **서브에이전트에 sonnet/haiku로 위임**한다.

| 작업 성격 | 담당 | 예시 |
|-----------|------|------|
| 아키텍처 설계, 상태 머신, 특징 추출 알고리즘, 프롬프트 설계, 씬 연출/디자인 판단, 디버깅 | **메인 세션 (opus)** | DataSource 추상화 설계, 시선 점수 공식 구현, 불일치 지수, 씬 전환 연출 |
| 보일러플레이트, 타입 정의 나열, 단순 CRUD, 반복적 컴포넌트, 설정 파일, 테스트 케이스 확장 | **서브에이전트 (`model: "sonnet"`)** | Supabase 클라이언트 래퍼, 폼/카드 컴포넌트, Tailwind 설정, 아두이노 스케치 |
| 단순 치환, 포맷 정리, 파일 목록화 | **서브에이전트 (`model: "haiku"`)** | import 정리, 상수 파일 생성 |

`Agent` 툴 호출 시 `model` 파라미터로 지정한다. 독립적인 작업은 한 메시지에서 병렬로 띄운다.

## 마일스톤

### M1 — 골격 + 시뮬레이터
- Next.js 15 + TypeScript + Tailwind v4 프로젝트 생성 (`npx create-next-app`)
- `src/lib/sensors/` : `DataSource` 인터페이스 + `SimulatedSource` + `ManualSource`
  - 시뮬레이터는 생리학적으로 그럴듯해야 한다: HR = 기저 72bpm + 호흡성 동성부정맥(0.25Hz) + 노이즈, GSR = 토닉 레벨 + 이벤트 시 SCR 파형(1~3초 상승 후 지수 감쇠)
  - **이벤트 버스와 연결**: 자극 제시·질문 시작 같은 체험 이벤트에 신호가 반응해야 한다
  - 가상 페르소나 4종(조용한 탐구자 / 무대 위의 리더 / 자연을 그리는 사람 / 정돈된 설계자)마다 반응 패턴이 달라야 한다
- Zustand 세션 상태 머신 (S0~S8 씬 전이, 타이머, 이벤트 마커 기록)
- **완료 기준**: 가상 참가자로 Phase 1→4 전체 플로우가 자동 진행됨

### M2 — 체험 씬 (S0~S6)
`구현계획.md` 3.2절의 씬 표를 그대로 구현. 3.2.1절의 지루함 방지 장치를 반드시 포함.
- Canvas 2D 실시간 파형 렌더러 (스크롤 파형 + 잔광)
- S2 호흡 가이드 원 (4초 주기, 안정 수렴 시 색온도 변화)
- S4 시선 세션: 이미지 쌍 12개, 6초 노출 + 2초 응시점, **좌우 무작위 반전**, 데모 모드는 마우스 위치를 시선 프록시로
- S5 그림 캔버스: Pointer Events, `pressure` 수집, 스트로크 타임스탬프 기록
- S6 대화: Web Speech API STT + TTS 낭독, 5문항 30초 리듬(낭독 5초 → 응답 20초 → 회복 5초)
- **완료 기준**: 사람이 처음부터 끝까지 완주 가능하고 지루하지 않음

### M3 — 해석 엔진
- `src/lib/features/` : 전부 **순수 함수 + 유닛 테스트**
  - `baseline.ts` / `gaze.ts` / `drawing.ts` / `dialogue.ts` / `crosscheck.ts`
  - 시선 점수 = `0.4 × 첫응시 + 0.6 × 체류비율`, 각성 동반 시 `×1.3`, 축별 -1~+1 정규화
  - 불일치 지수 = `|content_valence − arousal_z|`
- `buildLlmInput()` → 부록 C Input 스키마 JSON (결측은 `null` + `quality:"missing"`)
- `/api/interpret` : 부록 C System 프롬프트, zod 스키마 검증, 실패 시 1회 재시도, 2회 실패 시 규칙 기반 템플릿 폴백
- 발화 정서가(`content_valence`)는 **별도 호출**로 선평정
- S7(리플레이 쇼) · S8(결과) 씬
- **완료 기준**: 시뮬레이션 데이터로 그럴듯한 bio-receipt JSON이 나옴

### M4 — 배포 + 대시보드 + 영수증
- Supabase 스키마: `sessions` / `signal_chunks` / `events` / `receipts`
  - 환경변수 없으면 **localStorage 폴백**으로 동작할 것
- `/operator` 진행자 대시보드 + Supabase Realtime 동기화
- `/receipt/[sessionId]` + 80mm 인쇄 CSS (`@page { size: 80mm auto }`), 본인 심박 파형 인쇄, QR
- 랜딩 `/` : 비전 스토리텔링 (링크 하나가 제안서 역할)
- **3분 압축 모드** 토글
- Vercel 배포
- **완료 기준**: 공개 URL에서 전체 체험 완주

### M5 — 실기기 연동 (기기 배송 전이므로 코드만 준비)
- `BleHeartRateSource` (Web Bluetooth, GATT 0x180D, RR-interval → RMSSD 30초 슬라이딩 윈도우)
- `SerialGsrSource` (Web Serial 115200bps CSV 파싱) + `arduino/gsr_stream.ino` 스케치
- `WebGazerSource` (9점 캘리브레이션, 좌/우 절반 판정)
- 전부 인터페이스만 맞추면 교체되도록. 기기 없이 **연결 실패 시 시뮬레이터로 자동 폴백**
- **완료 기준**: 타입체크·빌드 통과, 연결 UI 동작 (실측은 배송 후)

### M6 — 폴리시
- 마이크로 인터랙션, 사운드(선택), 씬 전환 다듬기
- 자극 이미지: 저작권 안전한 소스(Unsplash/Pexels) 또는 플레이스홀더. **출처 명시**
- 부록 C 검수 체크리스트 수행 (근거 없는 문장 / 결측 처리 / mismatch null / 반복 안정성 / 금지 표현)
- README 작성 (실행법, 환경변수, 부스 세팅 절차)

## 반복 검증 루프

각 마일스톤마다:
1. 구현 → 2. `npx tsc --noEmit` + `npm run build` → 3. 유닛 테스트 → 4. **실제 플로우 수동 검증** → 5. 발견된 문제 수정 → 2번으로

브라우저 검증이 가능하면 개발 서버를 띄우고 실제로 클릭해 확인한다. 스크린샷으로 디자인을 눈으로 검토하고, "AI가 만든 것 같은" 결과가 나오면 다시 만든다.

## 완료 시

- 전체 마일스톤 완료 후 커밋·푸시
- 최종 요약 작성: 배포 URL, 구현된 것, 남은 것(실기기 실측·자극 이미지 확정), 팀이 결정해야 할 것
